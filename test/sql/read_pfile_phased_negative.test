# name: test/sql/read_pfile_phased_negative.test
# description: Negative tests for read_pfile phased parameter
# group: [sql]

require plinking_duck

# phased + dosages → incompatible
statement error
SELECT * FROM read_pfile('test/data/phased_example', phased := true, dosages := true);
----
dosages
